-- bouns Q2. List the Products Ordered in a Period
select product_name,sum(unit) as unit from Products p
join Orders o on p.product_id = o.product_id and month(order_date)=2 and year(order_date)=2020
group by product_name
having unit>=100